# Public backend origins only; do not put tokens, passwords, or other secrets here.
# The /api suffix is optional.
PRIMARY_API_URL=https://YOUR-PRIMARY-RENDER-URL
SECONDARY_API_URL=https://YOUR-SECONDARY-RENDER-URL
